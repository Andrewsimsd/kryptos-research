[package]
name = "kryptos-research"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Reproducible evidence and exact baseline diagnostics for Kryptos K4"
license = "MIT"

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
